
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: setu-backend
    environment:
      SPRING_DATASOURCE_URL: ${DATABASE_URL}
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
    ports:
      - "8080:8080"